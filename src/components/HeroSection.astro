---
import Keyboard from '@/components/Keyboard.astro'
---

<section class="flex h-screen flex-col gap-8 pb-32 pt-24 sm:justify-center">
  <div
    class="flex w-fit items-center rounded-full gap-3 py-1.5 pl-3 pr-4 border border-zinc-600/75 bg-slate-950/25 enter-animation"
  >
    <div class="relative size-3">
      <div class="absolute size-full animate-ping rounded-full bg-green-300/65" />
      <div class="drop-shadow-green size-full rounded-full bg-green-400" />
    </div>
    <h3 class="text-sm text-slate-400 max-xs:text-xs">
      Available for projects
    </h3>
  </div>
  
  <h1 class="enter-animation text-[clamp(2rem,7vw,3rem)] font-medium leading-[1.05] tracking-tight">
    Hi, I’m Amir. <br /> A software engineer.
  </h1>
  
  <h2 class="enter-animation mb-8 max-w-xl leading-relaxed text-slate-400 max-sm:text-sm">
    Mainly working in the Python ecosystem, I’m a dedicated problem-solver
    who thrives on learning and building.
  </h2>
  
  <Keyboard class="enter-animation [&:nth-child(4)]:animate-delay-[600ms]" />
</section>

<style>
  /* Move animations to Tailwind config */
  .enter-animation {
    opacity: 0.025;
    animation: fade-in-bottom 0.45s cubic-bezier(0.39, 0.575, 0.565, 1) forwards;
  }
  
  /* Use Tailwind's animation delay utilities instead */
  .enter-animation:nth-child(2) {
    animation-delay: 200ms;
  }
  .enter-animation:nth-child(3) {
    animation-delay: 400ms;
  }
  @tailwind base;
  @tailwind components;
  @tailwind utilities;
</style>